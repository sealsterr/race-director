Never create a component longer than 200 lines. If it exceeds this, split it into smaller components automatically. This rule should only be applied if the output & project doesn't become too complex and confusing. Prioritise user accessibility and readability over this rule. Avoid too many files and folders for minor changes / applications.

Always separate UI from logic.

When committing, write the most unhinged commit message, in the style of gen Z, based on my changes.
